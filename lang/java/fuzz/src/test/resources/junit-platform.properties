# Jazzer JUnit configuration
# Instrument Avro classes for coverage-guided fuzzing
jazzer.instrument=org.apache.avro.**
# To enable continuous fuzzing (not just regression testing), run:
#   mvn test -pl fuzz -Pfuzz
# or set environment variable JAZZER_FUZZ=1
